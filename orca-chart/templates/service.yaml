{{- if .Values.service.http.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm.fullname" . }}
  labels:
    {{- include "helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.http.port | default 80 }}
      targetPort: http
      protocol: TCP
      name: http
      {{- if and (.Values.service.http.nodePort) (eq .Values.service.type "NodePort") }}
      nodePort: {{ .Values.service.http.nodePort }}
      {{- end }}
    {{- if .Values.service.https.enabled }}
    - port: {{ .Values.service.https.port | default 443 }}
      targetPort: https
      protocol: TCP
      name: https
      {{- if and (.Values.service.https.nodePort) (eq .Values.service.type "NodePort") }}
      nodePort: {{ .Values.service.https.nodePort }}
      {{- end }}
    {{- end }}
  selector:
    {{- include "helm.selectorLabels" . | nindent 4 }}
{{- end -}}