{{- if and .Values.brainz.enabled .Values.brainz.autoscaling.enabled }}
{{- $kubeVersion := $.Capabilities.KubeVersion.Version }}
{{- $apiVersions := $.Capabilities.APIVersions }}
{{- if $kubeVersion | semverCompare ">=1.26.0-0" }}
apiVersion: autoscaling/v2
{{- else if $kubeVersion | semverCompare ">=1.23.0-0" | and ($apiVersions.Has "autoscaling/v2") }}
apiVersion: autoscaling/v2
{{- else if $kubeVersion | semverCompare ">=1.12.0-0" | and ($apiVersions.Has "autoscaling/v2beta2") }}
apiVersion: autoscaling/v2beta2
{{- else if $apiVersions.Has "autoscaling/v2beta1" }}
apiVersion: autoscaling/v2beta1
{{- else }}
apiVersion: autoscaling/v1
{{- end }}
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "varnish-controller.fullname" . }}-brainz
  labels:
    {{- include "varnish-controller.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "varnish-controller.fullname" . }}-brainz
  minReplicas: {{ .Values.brainz.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.brainz.autoscaling.maxReplicas }}
  {{- if not (empty .Values.brainz.autoscaling.behavior) }}
  {{- $tp := kindOf .Values.brainz.autoscaling.behavior }}
  behavior:
  {{- if eq $tp "string" }}
    {{- tpl .Values.brainz.autoscaling.behavior . | trim | nindent 4 }}
  {{- else }}
    {{- toYaml .Values.brainz.autoscaling.behavior | nindent 4 }}
  {{- end }}
  {{- end }}
  {{- if not (empty .Values.brainz.autoscaling.metrics) }}
  metrics:
  {{- $tp := kindOf .Values.brainz.autoscaling.metrics }}
  {{- if eq $tp "string" }}
    {{- tpl .Values.brainz.autoscaling.metrics . | trim | nindent 4 }}
  {{- else }}
    {{- toYaml .Values.brainz.autoscaling.metrics | nindent 4 }}
  {{- end }}
  {{- end }}
{{- end }}
